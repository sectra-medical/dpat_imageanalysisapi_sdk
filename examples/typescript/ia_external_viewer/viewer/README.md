# Frontend for Sectra DPAT IA-API Example: External Viewer

Simple React frontend that uses the Sectra DPAT IA-API to view the slides in a case.

## Configuration

Change the port in `vite.config.ts` if you change the port of the backend server.

## Running

Run using npm:

```sh
npm run dev
```
